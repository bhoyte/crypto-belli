export const UPDATE_INTERVAL = 60 * 60 * 1000; // 1 hour in milliseconds

export const CRYPTO_COLORS = {
  bitcoin: '#F7931A',
  ethereum: '#627EEA',
  // Add more crypto colors as needed
};

export const MAP_CONFIG = {
  width: 800,
  height: 600,
  scale: 150,
  center: [0, 20]
};
